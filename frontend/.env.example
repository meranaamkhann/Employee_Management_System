# Vite proxies /api to the backend in dev (see vite.config.ts).
# In production, set this to your deployed API's base URL if not served
# behind the same reverse proxy as the frontend.
VITE_API_BASE_URL=/api/v1
